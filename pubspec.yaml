name: voxstudio_ai
description: VoxStudio AI App with AdMob and AI Video
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_mobile_ads: ^5.0.0 # 👈 یہ AdMob کے لیے شامل کیا ہے
  http: ^1.2.0 # 👈 یہ فری API کنیکشن کے لیے ہے

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
